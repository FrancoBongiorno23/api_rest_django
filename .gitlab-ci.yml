image: python:1.10.4-python3

before_script:
  - python --version

stages:
  - test

test:
  stage: test
  script:
    - python3 --version
    - py manage.py test

    ##probamos testing solo mediante correr test
