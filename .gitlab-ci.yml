image: python

before_script:
  - python --version

stages:
  - test

test:
  stage: test
  script:
    - python3 --version
    - pip install Django==4.0.5
    - python manage.py test

    ##probamos testing solo mediante correr test
