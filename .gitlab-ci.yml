image: python

before_script:
  - python --version

stages:
  - test

test:
  stage: test
  script:
    - python3 --version
    - source -m venv/bin/activate
    - pip install requirements.txt
    - py manage.py makemigrations
    - py manage.py migrate
    - py manage.py test

    ##probamos testing solo mediante correr test
